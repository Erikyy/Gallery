import { FC } from 'react';

const Header: FC = () => {
  return (
    <header>
      <div className="flex flex-row">
        <div>Gallery</div>
      </div>
    </header>
  );
};

export default Header;
